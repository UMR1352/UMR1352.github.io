# The URL the site will be built for
base_url = "https://emarconi.xyz"
title = "Enrico Marconi"
description = "My personal website"

theme = "apollo"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "ayu-dark"

[extra]
# control dark mode: auto | dark | toggle
theme = "toggle"
# if set true, will use external CDN resource to load font and js file
use_cdn = false
favicon = "favicon-32x32.png"
menu = [
    { name = "/posts", url = "/posts", weight = 1 },
    { name = "/projects", url = "/projects", weight = 2 },
    { name = "/about", url = "/about", weight = 3 },
]

socials = [
    { name = "github", url = "https://github.com/umr1352/", icon = "github" },
    { name = "linkedin", url = "https://linkedin.com/in/enrico-marconi/", icon = "linkedin" },
]
